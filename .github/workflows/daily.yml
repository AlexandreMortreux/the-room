name: daily

on:
  schedule:
    - cron: "20 0 * * *" # 00:20 UTC = 08:20 UTC+8 (off the top-of-hour queue)
  workflow_dispatch:
    inputs:
      force:
        description: "Post for real (channel + ledger). Off = dry run, nothing published."
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  daily-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run daily digest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHANNEL_ID: ${{ secrets.TG_CHANNEL_ID }}
          REPO_URL: ${{ github.server_url }}/${{ github.repository }}
          # schedule -> real; manual -> dry unless force=true
          DRY_RUN: ${{ github.event_name == 'schedule' && '0' || '1' }}
          FORCE: ${{ github.event.inputs.force == 'true' && '1' || '0' }}
        run: python main.py

      - name: Commit ledger if changed
        run: |
          if ! git diff --quiet ledger.csv; then
            git config user.name "the-room-bot"
            git config user.email "the-room-bot@users.noreply.github.com"
            git add ledger.csv
            git commit -m "ledger: daily update $(date -u +%Y-%m-%d)"
            git push
          else
            echo "ledger unchanged, nothing to commit"
          fi
